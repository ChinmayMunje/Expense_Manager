<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.applycreditcard.expense_manager">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/accounting"
        android:label="@string/app_name"
        android:roundIcon="@drawable/accounting"
        android:supportsRtl="true"
        android:theme="@style/Theme.Expense_Manager">
        <!--        <activity android:name=".MainActivity" />-->
        <activity
            android:name=".login"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".registeration"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".splash"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Expense_Manager.NoActionBar" />
    </application>

</manifest>